# Dojo 1.7 common pattern snippets
snippet dr
	dojo.ready(function () {
		${1:console.log('dojo.ready');}
	}); // dojo.ready(...)

snippet dq
	dojo.query("${1:sel}")${2}

snippet dfe
	dojo.forEach(${1:arr}, function(${2:item}){
		$3
	});

snippet dh
	dojo.hitch(${1:this}, function(){
		$2
	});

snippet widget Dojo 1.6 Widget
	dojo.declare([dijit._Widget, dijit._Templated]{
		widgetsInTemplate: ${1:true},

		postCreate: function(){
			this.inherited(arguments);
			$2
		}
	});
